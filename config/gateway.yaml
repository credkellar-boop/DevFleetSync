gateway:
  name: "Video-Gateway-v1"
  port: 8080
  signaling_url: "wss://your-domain.com/ws"
  auth_required: true
  reputation_threshold: 500 # Integrates with your Deployma credentials
  logging:
    enabled: true
    path: "./logs/video_sessions.log"
