Deployma credentials
  logging:
    enabled: true
    path: "./logs/video_sessions.log"
gateway:
  name: "Video-Gateway-v1"
  port: 8080
  auth_enabled: true
  auth_service: "src/auth/validator"
  reputation_check: true
  session_timeout: 3600 # 1 hour
